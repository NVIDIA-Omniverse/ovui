# Block Quotes

## Simple

> A simple one-line quote.

## Multi-paragraph quote

> First paragraph of the quote. This paragraph is long enough to wrap
> across two visual lines, so we can verify that the quote bar
> continues through every wrapped line.
>
> Second paragraph inside the same quote. Separated by a blank
> continuation marker line.

## Nested — three levels

> Level 1 quote.
>
> > Level 2 quote nested inside level 1.
> >
> > > Level 3 quote, deepest. Each level should have its own distinct
> > > bar, and the indents should stack visibly.

## Quote containing a heading

> ### Heading inside a quote
>
> A paragraph below the quoted heading.

## Quote containing a list

> - list item inside a quote
> - another list item
>   - nested list item inside a quote

## Quote containing a fenced code block

> Quoted intro text.
>
> ```
> code block inside a quote
> second line of quoted code
> ```
>
> Quoted outro text.

## Quote with inline styling

> This quote has **bold**, *italic*, `inline code`, ~~strike~~, and a
> [link](https://example.com) all mixed together.
