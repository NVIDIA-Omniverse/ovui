Project layout:

- `core/`
  - `include/` — public headers
  - `src/` — implementation
    - `markdown/` — the native markdown widget
    - `layout/` — flexbox-like layout engine
- `python/` — Python bindings
- `tests/`
  - `unit/` — unit tests
  - `integration/` — end-to-end tests
