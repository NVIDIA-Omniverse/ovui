Three core principles:

- **Consistency across surfaces.** Every widget should behave identically whether it's rendered in the IDE or the standalone app.

- **Performance under load.** The renderer must maintain 60 FPS even with thousands of nodes in the scene graph.

- **Accessibility by default.** Keyboard navigation, screen reader support, and high-contrast modes are not opt-in.
